Set args.update_epochs, the number of epochs
Set args.num_envs, the number of actors
Set args.num_steps, the time horizon
Randomly initialize the actor and critic parameters θA, θC
Initialize the “old” actor network θAold
for i = 1, 2, . . . do
    Set θAold = θA
    for actor = 1, 2, . . . , args.num_envs do
        Run policy θAold in environment for args.num_steps time steps and collect the trajectories
        Compute GAE advantages using θAold
        Calculate Vtarπ,1, . . . , Vtarπ,T using the critic network θC and/or trajectory data
    end for
    Let batch with size args.num_envs*args.num_steps consist of the collected trajectories, advantages, and target V -values
    for epoch = 1, 2, . . . , args.update_epochs do
        for minibatch m in batch do
            The following are computed over the whole minibatch m
            Calculate rm(θA)
            Calculate JmCLIP(θA) using the advantages Am from the minibatch and rm(θA)
            Calculate entropies Hm using using the actor network θA
            Calculate policy loss:
            Lpol(θA) = JmCLIP(θA) − βHm
            Calculate predicted V -value Vˆ π(sm) using the critic network θC
            Calculate value loss using the V -targets from the minibatch:
            Update actor parameters and critic parameters jointly using SGD:
        end for
    end for
end for